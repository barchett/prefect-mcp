---
phase: 02-wiring-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - WIRE-02
  - WIRE-03
tags:
  - documentation
  - workflow
  - setup

must_haves:
  truths:
    - "CLAUDE.md is read by Claude Code at session start and tells it what tools are available, what loop to follow, and when to use fork/revert/abort"
    - "README.md describes a fresh-clone setup path that ends with `/mcp` showing prefect connected and the test task ready to run"
    - "README.md explicitly documents `npm install && npm run build` as a prerequisite step (because build/ is gitignored)"
    - "README.md specifies `opencode serve --port 4096` (with the port flag) — running without --port binds to a random port and breaks the MCP server's default OPENCODE_URL"
    - "CLAUDE.md states that opencode_approve_permission is an emergency-only tool because OpenCode's default config auto-approves permissions"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project-scoped instructions Claude Code reads at session start — defines the create -> run -> diff -> test -> correct loop"
      contains: "opencode_create_session"
    - path: "README.md"
      provides: "Human setup guide for fresh clone: install, build, configure OpenCode, run server, open Claude Code"
      contains: "npm run build"
  key_links:
    - from: "CLAUDE.md"
      to: "the 7 prefect tools registered in src/index.ts"
      via: "tool reference table"
      pattern: "opencode_(create_session|run|get_diff|fork|revert|abort|approve_permission)"
    - from: "README.md"
      to: ".mcp.json (created by plan 02-01)"
      via: "step that mentions or verifies the file"
      pattern: "\\.mcp\\.json"
    - from: "README.md"
      to: "examples/test-task.md (created by plan 02-01)"
      via: "validation step pointing to the test task"
      pattern: "examples/test-task\\.md"
    - from: "README.md"
      to: "OpenCode health endpoint"
      via: "health-check verification"
      pattern: "global/health"
---

<objective>
Write the two human-and-LLM-facing documents for Phase 2: `CLAUDE.md` (WIRE-02) tells Claude Code how to drive the prefect tools as a review/correct loop, and `README.md` (WIRE-03) tells a human operator (or a future fresh clone) how to set up the entire stack from scratch.

Purpose: These files convert a working MCP server (Phase 1) into a usable workflow. Without CLAUDE.md, Claude Code has the tools but no guidance on the canonical sequence. Without README.md, a fresh clone cannot complete setup deterministically (the build/ directory is gitignored, OpenCode needs a non-default `--port`, and OpenCode auto-approve config is non-obvious).

Output: `CLAUDE.md` and `README.md` at the project root, both committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wiring-validation/02-RESEARCH.md

<interfaces>
<!-- The 7 prefect tools registered in src/index.ts (verified Phase 1 SUMMARY). -->
<!-- CLAUDE.md must reference these by exact name and document their canonical sequence. -->

From src/index.ts (Phase 1 - working):
- opencode_create_session({title?}) -> Session.id
- opencode_run({sessionId, prompt}) -> AssistantMessage (blocks up to PREFECT_TIMEOUT_MS, default 120000ms)
- opencode_get_diff({sessionId, messageID?}) -> FileDiff[]
- opencode_approve_permission({sessionId, permissionId, response: 'once'|'always'|'reject'}) -> result
- opencode_fork({sessionId, messageID?}) -> Session
- opencode_revert({sessionId, messageID, partID?}) -> result
- opencode_abort({sessionId}) -> boolean

Env vars (consumed by src/index.ts):
- OPENCODE_URL (default: http://localhost:4096)
- PREFECT_TIMEOUT_MS (default: 120000)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CLAUDE.md with the canonical loop and tool reference (WIRE-02)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/02-wiring-validation/02-RESEARCH.md (section "WIRE-02: CLAUDE.md Loop Documentation" — this defines the 7-step loop, permission handling, and tool reference table; also "Pitfall 5: CLAUDE.md Instructions Too Vague")
    - src/index.ts (confirms exact tool names and input schemas — these must be referenced verbatim in CLAUDE.md)
    - .planning/PROJECT.md (Core Value statement and Key Decisions — auto-approve permissions decision must be reflected in CLAUDE.md)
  </read_first>
  <action>
    Create `CLAUDE.md` at the project root with EXACTLY the structure below. Substitute `<...>` placeholders with the literal content described inside them. Do not add sections, do not paraphrase the loop steps, do not omit the permission note.

    ```markdown
    # CLAUDE.md — Prefect Project Instructions

    This project is **Prefect**: a TypeScript MCP server (already built in `build/index.js`) that exposes OpenCode's HTTP API as Claude Code tools. When you (Claude Code) work on a task in this repo, you can delegate the actual file edits to a local model (Qwen via OpenCode) and review/correct the results.

    ## When to Use the Prefect Tools

    Use the prefect tools when:
    - The task is a coding task with a clear, scoped change (e.g. "implement function X", "fix bug Y").
    - The change is local to this working tree (OpenCode edits files in CWD).
    - You want to keep your context window free for high-level orchestration and review.

    Do NOT use the prefect tools for:
    - Reading or summarizing existing code (use the Read/Grep tools directly — faster, no extra hop).
    - Tasks that require multi-file architectural reasoning across the whole repo (do that yourself).
    - Tasks where you need to commit your own work (you commit; OpenCode just edits).

    ## The Canonical Loop

    Always follow this exact sequence. Do not improvise step ordering.

    1. **CREATE SESSION.** Call `opencode_create_session({title: "<short task name>"})`. Save the returned `id` as your session ID for the rest of the loop.
    2. **RUN PROMPT.** Call `opencode_run({sessionId, prompt: "<task description>"})`. This blocks until the agent finishes (default timeout 120 seconds; controlled by `PREFECT_TIMEOUT_MS`).
    3. **GET DIFF.** Call `opencode_get_diff({sessionId})`. Inspect the returned `FileDiff[]` to see what OpenCode changed.
    4. **REVIEW.** Read the diff. Read any modified files yourself if you need more context than the diff shows.
    5. **TEST.** Use the Bash tool to run the project's test/build commands (e.g. `npm run build`, `npm test` if it exists, or any task-specific verification). Do NOT delegate this to OpenCode — you run tests, you decide.
    6. **DECIDE.** Based on the diff and test results, choose ONE:
       - **Tests pass + diff is good** -> `git add <files> && git commit -m "<message>"`. Done.
       - **Tests fail or diff needs tweaks** -> `opencode_run({sessionId, prompt: "correct: <specific feedback>"})`. Go back to step 3.
       - **Session is off-rails (wrong files touched, model is confused)** -> `opencode_fork({sessionId, messageID: <id of last good message>})` to get a clean copy at a safe point, then go to step 2 with the new session ID.
       - **Single bad message to undo** -> `opencode_revert({sessionId, messageID: <bad message id>})`, then go to step 2.
       - **Give up entirely** -> `git checkout -- .` to reset the working tree; discard the session.
    7. **ABORT IF STUCK.** If `opencode_run` is taking too long and you want to stop it before the timeout, call `opencode_abort({sessionId})`.

    ## Permission Handling

    OpenCode's local config (`~/.config/opencode/opencode.json`) has all permissions set to `allow` (`bash`, `edit`, `write`, `webfetch`). This means OpenCode does NOT pause to ask for permission during normal operation.

    `opencode_approve_permission` is therefore an **emergency-only tool**. Use it only if:
    - OpenCode's config has been changed to require permissions (you'd see a `requestId` in the run output), OR
    - You explicitly want to deny a specific operation that did slip through.

    The default response is `once`, `always`, or `reject` (NOT `allow`/`deny`/`allow_always` — those are wrong despite some old docs).

    ## Tool Reference

    | Tool | Required Args | When to Call |
    |------|---------------|--------------|
    | `opencode_create_session` | `{title?: string}` | Once at the start of each task. |
    | `opencode_run` | `{sessionId, prompt}` | To send a task or correction. Blocks until OpenCode finishes (up to 120s). |
    | `opencode_get_diff` | `{sessionId, messageID?}` | After every `opencode_run` to see what changed. |
    | `opencode_abort` | `{sessionId}` | Emergency stop — when `opencode_run` is stuck. |
    | `opencode_fork` | `{sessionId, messageID?}` | Session went off-rails — fork at a safe point and continue. |
    | `opencode_revert` | `{sessionId, messageID, partID?}` | Undo a single bad message. |
    | `opencode_approve_permission` | `{sessionId, permissionId, response: 'once' \| 'always' \| 'reject'}` | Emergency only — auto-approve is the default. |

    ## Git Contract

    - All OpenCode work lands in the current working tree. There is no separate sandbox.
    - Git is the safety net. If a session produces bad output, `git checkout -- .` resets everything.
    - You commit. OpenCode edits files but does NOT commit. You review the diff first.

    ## Environment

    - The MCP server reads `OPENCODE_URL` (default `http://localhost:4096`) and `PREFECT_TIMEOUT_MS` (default `120000`).
    - For long-running tasks, set `PREFECT_TIMEOUT_MS` higher in the `env` field of `.mcp.json` and restart Claude Code.
    - OpenCode must be running: `opencode serve --port 4096`. Health check: `curl http://localhost:4096/global/health`.

    ## Validation

    For end-to-end validation that the loop works after a fresh setup, see `examples/test-task.md`.
    ```

    Notes:
    - The numbered loop is the canonical sequence — Pitfall 5 in RESEARCH.md says LLMs follow numbered sequences better than abstract descriptions.
    - All 7 tool names must be exact: `opencode_create_session`, `opencode_run`, `opencode_get_diff`, `opencode_approve_permission`, `opencode_fork`, `opencode_revert`, `opencode_abort`.
    - The permission response enum is `'once' | 'always' | 'reject'` (verified against the SDK in Phase 1; STATE.md captures this as a corrected requirement).
    - Do NOT mention models other than Qwen by name (the project's actual provider is vllm/Qwen3-Coder-30B per RESEARCH.md OpenCode config). Saying "Qwen via OpenCode" is correct.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && grep -q "opencode_create_session" CLAUDE.md && grep -q "opencode_run" CLAUDE.md && grep -q "opencode_get_diff" CLAUDE.md && grep -q "opencode_fork" CLAUDE.md && grep -q "opencode_revert" CLAUDE.md && grep -q "opencode_abort" CLAUDE.md && grep -q "opencode_approve_permission" CLAUDE.md && grep -q "once" CLAUDE.md && grep -q "always" CLAUDE.md && grep -q "reject" CLAUDE.md && grep -q "Canonical Loop" CLAUDE.md && grep -q "git checkout" CLAUDE.md && grep -q "OPENCODE_URL" CLAUDE.md && grep -q "PREFECT_TIMEOUT_MS" CLAUDE.md && grep -q "examples/test-task.md" CLAUDE.md && grep -q "emergency" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `CLAUDE.md` exists at project root
    - All 7 tool names appear: `grep -c "opencode_create_session\|opencode_run\|opencode_get_diff\|opencode_fork\|opencode_revert\|opencode_abort\|opencode_approve_permission" CLAUDE.md` is at least 7
    - The numbered canonical loop section exists: `grep -q "Canonical Loop" CLAUDE.md` exits 0
    - Permission enum values are documented: `grep -q "once" CLAUDE.md && grep -q "always" CLAUDE.md && grep -q "reject" CLAUDE.md` all exit 0
    - Permission tool is marked emergency-only: `grep -qi "emergency" CLAUDE.md` exits 0
    - Git safety-net pattern is documented: `grep -q "git checkout" CLAUDE.md` exits 0
    - Env vars referenced: `grep -q "OPENCODE_URL" CLAUDE.md && grep -q "PREFECT_TIMEOUT_MS" CLAUDE.md` exits 0
    - Cross-reference to validation task: `grep -q "examples/test-task.md" CLAUDE.md` exits 0
    - File does NOT use the wrong permission enum: `grep -E "allow_always|allow/deny" CLAUDE.md` exits non-zero
  </acceptance_criteria>
  <done>CLAUDE.md exists with the canonical 7-step loop, all 7 tool names, permission emergency note, env var references, and git safety contract — all greppable.</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md with end-to-end fresh-clone setup (WIRE-03)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-wiring-validation/02-RESEARCH.md (section "WIRE-03: README.md Setup Instructions" — defines verified command set; also Pitfalls 1, 2, 3 — `--scope project`, `npm run build` prerequisite, `--port 4096` requirement)
    - package.json (for the `build` script — must match in README)
    - .gitignore (to confirm build/ is excluded — README must explain why `npm run build` is needed)
    - .planning/phases/02-wiring-validation/02-01-PLAN.md (this confirms the .mcp.json and examples/test-task.md filenames the README cross-references)
    - .planning/PROJECT.md ("What This Is" and "Core Value" — README intro should match this framing)
  </read_first>
  <action>
    Create `README.md` at the project root with the structure below. The content uses verified commands from RESEARCH.md WIRE-03. Substitute `<...>` placeholders with the literal content described.

    ```markdown
    # Prefect

    A TypeScript MCP server that exposes OpenCode's headless HTTP API as Claude Code tools. Claude Code orchestrates at the task level (decompose, review, correct) while delegating actual file edits to a local model running in OpenCode. Diffs land in your working tree; you commit when ready.

    **Core value:** delegate implementation to a local model, review the diff in Claude Code, ship without context-switching.

    ## What's in the Box

    - 7 MCP tools wrapping OpenCode's session API: `opencode_create_session`, `opencode_run`, `opencode_get_diff`, `opencode_approve_permission`, `opencode_fork`, `opencode_revert`, `opencode_abort`.
    - Project-scoped Claude Code registration (`.mcp.json`) so any clone of this repo automatically picks up the tools.
    - End-to-end validation task (`examples/test-task.md`).

    ## Prerequisites

    - **Node.js >= 18** (tested on Node 20). `node --version` to check.
    - **OpenCode CLI >= 1.14**. Install: `curl -fsSL https://opencode.ai/install | bash`. Verify: `opencode --version`.
    - **Claude Code CLI**. Verify: `claude --version`.
    - A model endpoint OpenCode can talk to (vllm, Ollama, OpenAI-compatible, etc.). Configured in `~/.config/opencode/opencode.json`.

    ## Setup (Fresh Clone)

    ### 1. Clone and build the MCP server

    ```bash
    git clone <repo-url>
    cd supervisor
    npm install
    npm run build
    ```

    `npm run build` runs `tsc && chmod 755 build/index.js`. The `build/` directory is gitignored, so this step is REQUIRED on every fresh clone — Claude Code will fail to spawn the MCP server otherwise.

    ### 2. Verify the project-scoped MCP registration

    The repo ships with `.mcp.json` at the project root that registers the MCP server with Claude Code. To confirm it's there:

    ```bash
    cat .mcp.json
    ```

    You should see the `prefect` server configured with `command: "node"` and `args: ["build/index.js"]`. If `.mcp.json` is missing or empty, recreate it with:

    ```bash
    claude mcp add --scope project prefect -- node build/index.js
    ```

    > Use `--scope project`, not `--scope local`. Local scope stores the config in `~/.claude.json` (user-only, not committed); project scope writes `.mcp.json` so all clones get it.

    ### 3. Configure OpenCode

    OpenCode's config lives at `~/.config/opencode/opencode.json`. Example for a local vllm backend:

    ```json
    {
      "$schema": "https://opencode.ai/config.json",
      "provider": {
        "vllm": {
          "npm": "@ai-sdk/openai-compatible",
          "name": "vLLM (local)",
          "options": {
            "baseURL": "http://<your-vllm-host>:8000/v1"
          },
          "models": {
            "<model-id>": { "name": "Your Model" }
          }
        }
      },
      "model": "vllm/<model-id>",
      "permission": {
        "bash": "allow",
        "edit": "allow",
        "write": "allow",
        "webfetch": "allow"
      }
    }
    ```

    The `permission: allow` block is intentional — Prefect treats git as the safety net. If you want manual permission prompts, see `opencode_approve_permission` in `CLAUDE.md` (emergency tool).

    Auth file (placeholder is required even for local models):

    ```bash
    mkdir -p ~/.config/opencode
    echo '{"vllm": "dummy"}' > ~/.local/share/opencode/auth.json
    ```

    Adjust the provider key (`vllm`) and path if you use Ollama, OpenAI, etc.

    ### 4. Start OpenCode headless

    In a dedicated terminal:

    ```bash
    opencode serve --port 4096
    ```

    > **You must specify `--port 4096`.** The default port is `0` (random), which won't match the MCP server's default `OPENCODE_URL=http://localhost:4096`.

    Health check (in another terminal):

    ```bash
    curl http://localhost:4096/global/health
    # {"healthy":true,"version":"1.14.x"}
    ```

    ### 5. Open Claude Code

    From the project root:

    ```bash
    claude
    ```

    Inside the session, run:

    ```
    /mcp
    ```

    You should see `prefect` listed as connected. If it shows as failed, the most likely causes (in order):
    1. `build/index.js` does not exist -> run `npm run build`.
    2. `.mcp.json` is malformed or missing -> see step 2 above.
    3. OpenCode is not running on port 4096 -> see step 4.

    ### 6. Run the validation task

    With everything wired up, follow `examples/test-task.md` to confirm the full create -> run -> diff -> commit loop works end-to-end. Success means a new `examples/hello.ts` file lands in your working tree and you can commit it.

    ## Configuration

    | Env Var | Default | Purpose |
    |---------|---------|---------|
    | `OPENCODE_URL` | `http://localhost:4096` | Base URL for OpenCode API |
    | `PREFECT_TIMEOUT_MS` | `120000` | Max wait for `opencode_run` to return (ms) |

    To override per-project, edit the `env` field of `.mcp.json`:

    ```json
    "env": {
      "OPENCODE_URL": "http://192.168.x.x:4096",
      "PREFECT_TIMEOUT_MS": "300000"
    }
    ```

    ## Day-to-Day Use

    See `CLAUDE.md` for the canonical create -> run -> diff -> test -> correct loop. Claude Code reads `CLAUDE.md` automatically at session start, so you don't need to repeat the instructions.

    ## WSL Note

    If Claude Code runs inside WSL2 and OpenCode also runs inside WSL2, `localhost:4096` works as expected. If OpenCode is on the Windows host and you're using WSL2 default NAT networking, point `OPENCODE_URL` at the Windows host IP instead of `localhost`.

    ## Project Layout

    ```
    .
    ├── src/index.ts         # MCP server (7 tools)
    ├── build/               # Compiled output (gitignored)
    ├── .mcp.json            # Project-scoped Claude Code registration
    ├── CLAUDE.md            # Loop instructions for Claude Code
    ├── examples/
    │   └── test-task.md     # End-to-end validation prompt
    ├── package.json
    └── tsconfig.json
    ```

    ## Troubleshooting

    | Symptom | Cause | Fix |
    |---------|-------|-----|
    | `/mcp` shows prefect as failed | `build/` missing | `npm run build` then restart Claude Code |
    | `opencode_create_session` returns connection error | OpenCode not running, or wrong port | `opencode serve --port 4096`, verify with `curl http://localhost:4096/global/health` |
    | `opencode_run` times out | Default 120s exceeded | Increase `PREFECT_TIMEOUT_MS` in `.mcp.json` env |
    | `opencode_get_diff` returns `[]` | Prompt didn't ask OpenCode to write files | Re-prompt explicitly asking for a file write (see `examples/test-task.md` for a known-good prompt) |
    | Tools missing in fresh Claude session | `.mcp.json` not committed or wrong scope | `claude mcp add --scope project prefect -- node build/index.js` |
    ```

    Notes:
    - All commands are verified live per RESEARCH.md WIRE-03 — do NOT change them.
    - The `--port 4096` warning (Pitfall 3) must be in the OpenCode start step, formatted as a callout.
    - The `npm run build` prerequisite (Pitfall 2) must be in step 1 with the explicit "build/ is gitignored" explanation.
    - The `--scope project` warning (Pitfall 1) must be in step 2 with the explanation about `~/.claude.json` vs `.mcp.json`.
    - Cross-reference both `CLAUDE.md` and `examples/test-task.md` (these exist or will exist as products of this phase).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "npm install" README.md && grep -q "npm run build" README.md && grep -q "opencode serve --port 4096" README.md && grep -q "global/health" README.md && grep -q "\.mcp\.json" README.md && grep -q "scope project" README.md && grep -q "CLAUDE.md" README.md && grep -q "examples/test-task.md" README.md && grep -q "OPENCODE_URL" README.md && grep -q "PREFECT_TIMEOUT_MS" README.md && grep -q "Prerequisites" README.md && grep -q "Troubleshooting" README.md && grep -q "build/" README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `README.md` exists at project root
    - Setup commands present: `grep -q "npm install" README.md && grep -q "npm run build" README.md` exits 0
    - OpenCode start command with port: `grep -q "opencode serve --port 4096" README.md` exits 0
    - Health check command present: `grep -q "global/health" README.md` exits 0
    - `.mcp.json` referenced: `grep -q "\.mcp\.json" README.md` exits 0
    - `--scope project` warning present (Pitfall 1): `grep -q "scope project" README.md` exits 0
    - `npm run build` prerequisite explanation references gitignore (Pitfall 2): `grep -A2 "npm run build" README.md | grep -q "gitignore\|gitignored"` exits 0
    - Cross-references to other Phase 2 outputs: `grep -q "CLAUDE.md" README.md && grep -q "examples/test-task.md" README.md` exits 0
    - Env vars documented: `grep -q "OPENCODE_URL" README.md && grep -q "PREFECT_TIMEOUT_MS" README.md` exits 0
    - "Prerequisites" section present: `grep -q "## Prerequisites\|^### Prerequisites" README.md` exits 0
    - "Troubleshooting" section present: `grep -qi "Troubleshooting" README.md` exits 0
    - 7 tools listed in some form: `grep -c "opencode_create_session\|opencode_run\|opencode_get_diff\|opencode_fork\|opencode_revert\|opencode_abort\|opencode_approve_permission" README.md` is at least 7
  </acceptance_criteria>
  <done>README.md exists with 6-step setup (install -> build -> verify .mcp.json -> configure OpenCode -> serve -> open Claude), all 3 pitfall warnings inline, env-var table, troubleshooting table, and cross-references to CLAUDE.md and examples/test-task.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README documentation -> human operator commands | A user who copy-pastes commands trusts that the README does not contain hostile shell snippets |
| CLAUDE.md instructions -> Claude Code session | CLAUDE.md is read at session start and biases Claude Code's behavior; ambiguous instructions could lead to unintended tool calls or loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T (Tampering) | README install instructions | accept | The install command `curl -fsSL https://opencode.ai/install | bash` pipes a remote script to bash. This is the official OpenCode install path. Personal-use tool; user can review the script themselves before running. We do not host or proxy this. |
| T-02-02-02 | I (Information Disclosure) | README env var examples | mitigate | Example `.mcp.json env` block uses placeholder values like `192.168.x.x:4096` (LAN IP from the actual research dev machine, not credentials). No real secrets in README. |
| T-02-02-03 | R (Repudiation) | CLAUDE.md loop -> git commits | mitigate | CLAUDE.md mandates that Claude Code (not OpenCode) commits, after reviewing the diff. This keeps a clear authorship trail in git and prevents OpenCode from committing rogue changes. |
| T-02-02-04 | T (Tampering) | CLAUDE.md instructing `git checkout -- .` | accept | This command is in the loop's "give up" branch and discards local working-tree changes only. The user (or Claude) chooses to invoke it after a failed session — same blast radius as any local git reset. Documented behavior, not silent. |
</threat_model>

<verification>
After both tasks complete:
- `cat CLAUDE.md` shows the canonical loop section, all 7 tool names, and the permission emergency note
- `cat README.md` shows the 6-step setup with all 3 pitfall warnings (npm run build, --scope project, --port 4096)
- `grep -l "examples/test-task.md" CLAUDE.md README.md` lists both files (cross-reference intact)
- `grep -l "opencode_create_session" CLAUDE.md README.md` lists both files
- A reader following README.md from step 1 to step 6 has all the commands needed to reach a working state
</verification>

<success_criteria>
- WIRE-02 satisfied: CLAUDE.md documents the create -> run -> diff -> test -> correct loop with all 7 tools, permission semantics, and the git safety contract — all in numbered, unambiguous steps (Pitfall 5 mitigated)
- WIRE-03 satisfied: README.md gives a fresh clone the exact command sequence to reach `/mcp` showing prefect connected, with Pitfalls 1, 2, 3 explicitly called out as warnings inline with the relevant step
- Both files independent of plan 02-01 (no overlap in `files_modified`); plans run in parallel
- Acceptance criteria for both tasks pass via grep checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-wiring-validation/02-02-SUMMARY.md` documenting:
- Final structure of CLAUDE.md (section list)
- Final structure of README.md (section list)
- Confirmation that all 7 tool names appear in each file
- Any deviation from RESEARCH.md verified commands (there should be none)
- Cross-reference check: README points to CLAUDE.md and examples/test-task.md; CLAUDE.md points to examples/test-task.md
</output>
