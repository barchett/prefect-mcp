---
phase: 01-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - .planning/phases/01-mcp-server/01-01-SDK-METHODS.md
autonomous: true
requirements:
  - CORE-08
tags:
  - typescript
  - mcp
  - scaffolding
  - npm

must_haves:
  truths:
    - "Running `npm install` resolves all dependencies without errors"
    - "Running `npx tsc --noEmit` completes without errors when src/ is empty (config is valid)"
    - "node_modules/@opencode-ai/sdk/dist/gen/sdk.gen.d.ts exists and has been inspected to confirm actual method names on the Session class"
    - "package.json has type: module and a build script"
    - ".gitignore excludes node_modules/ and build/"
  artifacts:
    - path: "package.json"
      provides: "Node.js project manifest with all 4 dependencies pinned at researched versions"
      contains: '"type": "module"'
    - path: "tsconfig.json"
      provides: "TypeScript compiler config for ESM Node16 build to ./build"
      contains: '"module": "Node16"'
    - path: ".gitignore"
      provides: "Excludes node_modules and build output from git"
      contains: "node_modules"
    - path: ".planning/phases/01-mcp-server/01-01-SDK-METHODS.md"
      provides: "Confirmed SDK method names for Plans 02 and 03 to use without ambiguity"
      contains: "sessionCreate"
  key_links:
    - from: "package.json"
      to: "@opencode-ai/sdk@1.14.25"
      via: "npm dependencies"
      pattern: '"@opencode-ai/sdk": "1.14.25"'
    - from: "package.json"
      to: "@modelcontextprotocol/sdk@1.29.0"
      via: "npm dependencies"
      pattern: '"@modelcontextprotocol/sdk": "1.29.0"'
    - from: "tsconfig.json"
      to: "ESM Node16 module resolution"
      via: "module + moduleResolution settings"
      pattern: '"moduleResolution": "Node16"'
---

<objective>
Scaffold the TypeScript MCP server project: write package.json with locked dependency versions, write tsconfig.json for ESM Node16, install dependencies, and resolve the one open assumption from research (Assumption A2/A3 in 01-RESEARCH.md) by inspecting the installed `@opencode-ai/sdk` to record the exact method names the next plans will call.

Purpose: Plans 02 and 03 must be able to write tool handlers without guessing SDK method names. This plan eliminates that uncertainty up front and produces a runnable npm/tsc environment.

Output: package.json, tsconfig.json, .gitignore, node_modules/ (installed), and 01-01-SDK-METHODS.md (a short reference for downstream plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-server/01-RESEARCH.md
@.planning/phases/01-mcp-server/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write package.json, tsconfig.json, and .gitignore</name>
  <files>package.json, tsconfig.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-mcp-server/01-RESEARCH.md (Pattern 4, Pattern 5, Standard Stack table for exact versions)
    - .planning/phases/01-mcp-server/01-PATTERNS.md (the package.json and tsconfig.json blocks under the "Pattern Assignments" section)
  </read_first>
  <action>
    Create three files at the project root with EXACT content below — do not improvise or add fields not listed.

    **package.json** (CORE-08 — establishes the project that consumes OPENCODE_URL):
    ```json
    {
      "name": "prefect",
      "version": "1.0.0",
      "type": "module",
      "scripts": {
        "build": "tsc && chmod 755 build/index.js"
      },
      "bin": {
        "prefect": "./build/index.js"
      },
      "dependencies": {
        "@modelcontextprotocol/sdk": "1.29.0",
        "@opencode-ai/sdk": "1.14.25",
        "zod": "4.3.6"
      },
      "devDependencies": {
        "typescript": "6.0.3",
        "@types/node": "latest"
      }
    }
    ```

    **tsconfig.json** (RESEARCH.md Pattern 5):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "Node16",
        "moduleResolution": "Node16",
        "outDir": "./build",
        "rootDir": "./src",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true
      },
      "include": ["src/**/*"],
      "exclude": ["node_modules"]
    }
    ```

    **.gitignore** (project hygiene — node_modules and build output must not be committed):
    ```
    node_modules/
    build/
    *.log
    .DS_Store
    ```

    Notes:
    - "type": "module" is REQUIRED because the MCP SDK uses ESM imports with .js extensions in import paths.
    - "module": "Node16" + "moduleResolution": "Node16" is REQUIRED for the import path "@modelcontextprotocol/sdk/server/index.js" to resolve.
    - Do NOT add "main" — the entry point is "bin" + "build/index.js" only.
    - Do NOT add a "test" script — testing strategy is shell-based verification per RESEARCH.md.
  </action>
  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f .gitignore && grep -q '"type": "module"' package.json && grep -q '"module": "Node16"' tsconfig.json && grep -q '"moduleResolution": "Node16"' tsconfig.json && grep -q '"@opencode-ai/sdk": "1.14.25"' package.json && grep -q '"@modelcontextprotocol/sdk": "1.29.0"' package.json && grep -q '"zod": "4.3.6"' package.json && grep -q '^node_modules/' .gitignore && grep -q '^build/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` exists at project root
    - File `tsconfig.json` exists at project root
    - File `.gitignore` exists at project root
    - `grep -q '"type": "module"' package.json` exits 0
    - `grep -q '"@modelcontextprotocol/sdk": "1.29.0"' package.json` exits 0
    - `grep -q '"@opencode-ai/sdk": "1.14.25"' package.json` exits 0
    - `grep -q '"zod": "4.3.6"' package.json` exits 0
    - `grep -q '"typescript": "6.0.3"' package.json` exits 0
    - `grep -q '"module": "Node16"' tsconfig.json` exits 0
    - `grep -q '"moduleResolution": "Node16"' tsconfig.json` exits 0
    - `grep -q '"target": "ES2022"' tsconfig.json` exits 0
    - `grep -q '"outDir": "./build"' tsconfig.json` exits 0
    - `grep -q '^node_modules/' .gitignore` exits 0
    - `grep -q '^build/' .gitignore` exits 0
  </acceptance_criteria>
  <done>All three files exist at project root with the exact content above; all grep checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and confirm SDK method names</name>
  <files>node_modules/ (created by npm), package-lock.json (created by npm), .planning/phases/01-mcp-server/01-01-SDK-METHODS.md</files>
  <read_first>
    - package.json (just written in Task 1 — confirms what we are installing)
    - .planning/phases/01-mcp-server/01-RESEARCH.md (Assumptions Log A2/A3 — the open question this task closes; "Code Examples" section showing assumed method names sessionCreate, sessionPrompt, sessionAbort, etc.)
    - .planning/phases/01-mcp-server/01-PATTERNS.md (Shared Patterns -> "SDK Method Name Uncertainty")
  </read_first>
  <action>
    Step 1 — Install dependencies:
    ```bash
    npm install
    ```
    This must complete without errors. If it fails, stop and report; do not proceed.

    Step 2 — Confirm TypeScript scaffolding works (config is valid even with no source files):
    ```bash
    mkdir -p src
    npx tsc --noEmit
    ```
    `tsc --noEmit` with no src files should exit 0 (or print "no inputs were found" — both are acceptable, the config itself must not error). If it errors on the config itself, fix the config.

    Step 3 — Inspect the installed `@opencode-ai/sdk` to extract the actual method names on the `Session` class. The path is `node_modules/@opencode-ai/sdk/dist/gen/sdk.gen.d.ts`. Run:
    ```bash
    grep -n "session[A-Z]" node_modules/@opencode-ai/sdk/dist/gen/sdk.gen.d.ts | head -80
    ```
    If that file path doesn't exist, locate it:
    ```bash
    find node_modules/@opencode-ai/sdk -name "*.d.ts" | head -20
    ```
    Then grep the discovered file(s) for method names matching the 7 endpoints we need.

    Step 4 — Write `.planning/phases/01-mcp-server/01-01-SDK-METHODS.md` with the EXACT confirmed method name for each of the 7 OpenCode endpoints, in this exact table format:

    ```markdown
    # Confirmed @opencode-ai/sdk Method Names

    Source file: node_modules/@opencode-ai/sdk/<path-to-sdk.gen.d.ts>
    Confirmed: <date>

    | OpenCode Endpoint | SDK Method on `client.session` | MCP Tool That Uses It |
    |-------------------|--------------------------------|-----------------------|
    | POST /session | <confirmed name, e.g. sessionCreate> | opencode_create_session |
    | POST /session/{id}/message | <confirmed name> | opencode_run |
    | GET /session/{id}/diff | <confirmed name> | opencode_get_diff |
    | POST /session/{id}/permissions/{permId} | <confirmed name> | opencode_approve_permission |
    | POST /session/{id}/fork | <confirmed name> | opencode_fork |
    | POST /session/{id}/revert | <confirmed name> | opencode_revert |
    | POST /session/{id}/abort | <confirmed name> | opencode_abort |

    ## Notes
    - Permission response enum (POST /session/{id}/permissions/{permId}): "once" | "always" | "reject" (NOT allow/deny/allow_always)
    - opencode_run blocks until the agent loop completes — no AbortController, no timeout
    - Importable types found: <list any types like Session, FileDiff, AssistantMessage, Part>
    ```

    If any endpoint's method name cannot be located in the SDK type file, mark it as `UNRESOLVED — needs runtime check` in the table and document what was searched. Do NOT guess.
  </action>
  <verify>
    <automated>test -d node_modules/@opencode-ai/sdk && test -d node_modules/@modelcontextprotocol/sdk && test -d node_modules/zod && test -f .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_create_session" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_run" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_abort" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_get_diff" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_approve_permission" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_fork" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q "opencode_revert" .planning/phases/01-mcp-server/01-01-SDK-METHODS.md && grep -q '"once"' .planning/phases/01-mcp-server/01-01-SDK-METHODS.md</automated>
  </verify>
  <acceptance_criteria>
    - Directory `node_modules/@opencode-ai/sdk` exists
    - Directory `node_modules/@modelcontextprotocol/sdk` exists
    - Directory `node_modules/zod` exists
    - File `.planning/phases/01-mcp-server/01-01-SDK-METHODS.md` exists
    - The SDK methods doc lists all 7 tool names (`opencode_create_session`, `opencode_run`, `opencode_get_diff`, `opencode_approve_permission`, `opencode_fork`, `opencode_revert`, `opencode_abort`) — verifiable via grep for each
    - The doc records the permission enum as `"once" | "always" | "reject"` — `grep -q '"once"' .planning/phases/01-mcp-server/01-01-SDK-METHODS.md` exits 0
    - `npx tsc --noEmit` runs without errors caused by the config itself (no input files is acceptable)
  </acceptance_criteria>
  <done>node_modules populated with all dependencies; SDK methods reference doc written and lists confirmed names for all 7 endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User shell -> npm registry | npm install pulls third-party packages from the public registry over TLS |
| Project filesystem -> committed git history | .gitignore prevents node_modules and build output from polluting git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | T (Tampering) | npm dependency tree | accept | Personal-use tool; versions are pinned exactly (no ^ or ~ ranges) so future installs are deterministic; package-lock.json is generated and can be committed/audited |
| T-01-01-02 | I (Information Disclosure) | git history | mitigate | .gitignore excludes node_modules/ and build/ — prevents accidental commit of installed packages or compiled output that could include local paths |
| T-01-01-03 | E (Elevation of Privilege) | npm postinstall scripts | accept | Personal-use tool on developer machine; the 4 packages chosen (@modelcontextprotocol/sdk, @opencode-ai/sdk, zod, typescript) are reputable; not introducing security_enforcement controls per RESEARCH.md security domain note |
</threat_model>

<verification>
After both tasks complete:
- `ls package.json tsconfig.json .gitignore` lists all three files
- `ls node_modules/@opencode-ai/sdk node_modules/@modelcontextprotocol/sdk node_modules/zod` lists all three packages
- `cat .planning/phases/01-mcp-server/01-01-SDK-METHODS.md` shows a populated table for all 7 tools
- `npx tsc --noEmit` does not error on the tsconfig itself
</verification>

<success_criteria>
- All three config files written with exact content from research patterns
- All four dependencies (3 prod + 1 dev + @types/node) installed at the pinned versions
- SDK method name uncertainty (Assumption A2/A3 from research) is closed: a reference doc exists that Plans 02 and 03 can copy method names from without inspecting node_modules themselves
- Plan 02 can begin immediately by reading 01-01-SDK-METHODS.md and writing src/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-server/01-01-SUMMARY.md` documenting:
- Confirmed SDK method names (copy the table from 01-01-SDK-METHODS.md)
- Whether `tsc --noEmit` ran cleanly
- Any deviations from RESEARCH.md (e.g., if a pinned version was unavailable, what was substituted and why)
</output>
