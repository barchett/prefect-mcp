---
phase: 09-npm-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/cli.ts
  - CLAUDE.md
  - README.md
  - examples/test-task.md
autonomous: true
requirements:
  - DIST-04
  - DIST-05
  - DIST-06
  - DIST-07
  - DIST-09
  - DIST-10
  - DIST-11
  - DIST-12
tags:
  - npm-publishing
  - global-install
  - documentation

must_haves:
  truths:
    - "src/cli.ts detects global vs local install via path-segment check on import.meta.url"
    - "When globally installed, prefect init writes mcpServers.prefect = { command: 'prefect-mcp', args: [], ... }"
    - "When locally installed, prefect init writes mcpServers.prefect = { command: 'node', args: ['/abs/path/build/index.js'], ... } (existing behavior)"
    - "CLAUDE.md tool reference table and canonical loop use prefect_* names throughout"
    - "CLAUDE.md canonical loop step 1 instructs callers to always pass an explicit directory arg on prefect_create_session, prefect_delegate, and prefect_dispatch"
    - "README.md documents two install pathways (local clone+build AND global npm install -g prefect-mcp) with example .mcp.json snippets for each"
    - "README.md env var table uses PREFECT_* names with note about deprecated OPENCODE_* fallback"
    - "examples/test-task.md uses prefect_* tool names"
    - "npm pack --dry-run output contains build/ files and README.md and excludes src/ TypeScript and node_modules/"
    - "grep -rn opencode_ over CLAUDE.md, README.md, examples/test-task.md returns zero matches"
    - "grep -rn OPENCODE_ over CLAUDE.md, README.md, examples/test-task.md matches only the deprecation/migration mentions (not canonical instructions)"
  artifacts:
    - path: "src/cli.ts"
      provides: "Global install detection + two-mode PREFECT_ENTRY (global=prefect-mcp bin, local=node + abs path)"
      contains: "isGlobal"
    - path: "CLAUDE.md"
      provides: "Canonical loop with prefect_* names + explicit directory arg instruction"
      contains: "prefect_create_session"
    - path: "README.md"
      provides: "Both install pathways documented with .mcp.json snippets and PREFECT_* env var table"
      contains: "npm install -g prefect-mcp"
    - path: "examples/test-task.md"
      provides: "Validation prompt using prefect_* tool names"
      contains: "prefect_create_session"
  key_links:
    - from: "src/cli.ts"
      to: "build/index.js (global) OR node + absolute path (local)"
      via: "path-segment isGlobal check then conditional PREFECT_ENTRY"
      pattern: "isGlobal\\s*\\?\\s*\\{[^}]*prefect-mcp"
    - from: "CLAUDE.md canonical loop step 1"
      to: "directory arg requirement"
      via: "instruction text under CREATE SESSION step"
      pattern: "always pass.*directory"
    - from: "README.md install section"
      to: "global install pathway"
      via: "section heading + npm install command + .mcp.json snippet"
      pattern: "npm install -g prefect-mcp"
---

<objective>
Wire global install detection into src/cli.ts (DIST-05), update all documentation files (CLAUDE.md, README.md, examples/test-task.md) to use prefect_* tool names and PREFECT_* env var names (DIST-07, DIST-09, DIST-10, DIST-12), add a global install pathway section to README.md (DIST-06), add the explicit-directory-arg instruction to CLAUDE.md canonical loop (DIST-11), and verify the npm tarball contents via `npm pack --dry-run` (DIST-04).

Purpose: With the code rename and package.json shape locked in by Plan 1 (Wave 1) and proven by `npm test`, this wave focuses on (a) the cli.ts global-detection wiring that depends on the new package.json `name: prefect-mcp` and second bin entry, and (b) the documentation surface that callers see. After this plan, the package is ready for `npm publish` as a manual human step.

Output: Updated cli.ts with two-mode PREFECT_ENTRY, three documentation files reflecting the new names and conventions, and a verified `npm pack --dry-run` output confirming the package only ships build/ + README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-npm-distribution/09-CONTEXT.md
@.planning/phases/09-npm-distribution/09-RESEARCH.md
@.planning/phases/09-npm-distribution/09-PATTERNS.md
@CLAUDE.md
@README.md
@examples/test-task.md
@src/cli.ts
@package.json

<interfaces>
<!-- Patterns extracted from RESEARCH.md (verified across version managers). -->

Pattern 3 - Global Install Detection (path-segment, ESM-compatible):

```
import { fileURLToPath } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const isGlobal = __dirname.replace(/\\/g, '/').includes('/node_modules/prefect-mcp/');
```

The `.replace(/\\/g, '/')` normalization handles Windows backslash paths. The path-segment check works across nvm, asdf, volta, standard npm, and Windows AppData/Roaming paths because Node resolves symlinks before computing import.meta.url - the resolved path is always the real file path inside node_modules/.

Pattern 4 - Two-mode PREFECT_ENTRY in src/cli.ts:

```
const PREFECT_ENTRY = isGlobal
  ? {
      type: 'stdio' as const,
      command: 'prefect-mcp',
      args: [],
      env: {},
    }
  : {
      type: 'stdio' as const,
      command: 'node',
      args: [resolve(__dirname, 'index.js')],
      env: {},
    };
```

Existing imports in src/cli.ts already cover this: `dirname`, `fileURLToPath`, `resolve` are imported on lines 2-4. The `mcpServerPath` intermediate variable (line 10 in current file) is no longer needed - inline `resolve(__dirname, 'index.js')` directly in the local branch.

Tool rename mapping (all 25, exact - SAME mapping as Plan 1, applied to docs this time):

```
opencode_create_session    -> prefect_create_session
opencode_abort             -> prefect_abort
opencode_run               -> prefect_run
opencode_prompt_async      -> prefect_prompt_async
opencode_get_diff          -> prefect_get_diff
opencode_approve_permission-> prefect_approve_permission
opencode_fork              -> prefect_fork
opencode_revert            -> prefect_revert
opencode_session_list      -> prefect_session_list
opencode_session_get       -> prefect_session_get
opencode_session_status    -> prefect_session_status
opencode_session_messages  -> prefect_session_messages
opencode_session_message   -> prefect_session_message
opencode_session_delete    -> prefect_session_delete
opencode_session_rename    -> prefect_session_rename
opencode_session_children  -> prefect_session_children
opencode_session_unrevert  -> prefect_session_unrevert
opencode_session_command   -> prefect_session_command
opencode_delegate          -> prefect_delegate
opencode_dispatch          -> prefect_dispatch
opencode_inspect           -> prefect_inspect
opencode_await             -> prefect_await
opencode_list_agents       -> prefect_list_agents
opencode_list_providers    -> prefect_list_providers
opencode_find_symbol       -> prefect_find_symbol
```

Env var rename mapping (D-07, exact):

```
OPENCODE_URL              -> PREFECT_SERVER_URL
OPENCODE_SERVER_PASSWORD  -> PREFECT_SERVER_PASSWORD
OPENCODE_SERVER_USERNAME  -> PREFECT_SERVER_USERNAME
OPENCODE_DEFAULT_PROJECT  -> PREFECT_DEFAULT_PROJECT
PREFECT_TIMEOUT_MS        -> unchanged
PREFECT_AUTOSTART_TIMEOUT_MS -> unchanged
```

README global install snippet template (Pattern from existing README .mcp.json local pattern, mirrored for global):

```
## Install

### Option 1: Global install (recommended)

    npm install -g prefect-mcp
    cd /your/project
    prefect init

`prefect init` auto-detects the global install and writes:

    {
      "mcpServers": {
        "prefect": {
          "type": "stdio",
          "command": "prefect-mcp",
          "args": [],
          "env": {}
        }
      }
    }

### Option 2: Local clone (development / contributing)

    git clone https://github.com/<user>/prefect-mcp.git
    cd prefect-mcp
    npm install
    npm run build
    cd /your/project
    /path/to/prefect-mcp/build/cli.js init

`prefect init` (run from a local clone) detects the absence of the `node_modules/prefect-mcp/` path segment and writes:

    {
      "mcpServers": {
        "prefect": {
          "type": "stdio",
          "command": "node",
          "args": ["/abs/path/to/prefect-mcp/build/index.js"],
          "env": {}
        }
      }
    }
```

CLAUDE.md canonical loop directory-arg instruction (DIST-11) - to be added to step 1 (CREATE SESSION):

```
1. **CREATE SESSION.** Call `prefect_create_session({title: "<short task name>", directory: "<absolute path to your project>"})`. Save the returned `id` as your session ID for the rest of the loop. **Always pass `directory` explicitly** - never rely on the server's default working directory. The same applies to `prefect_delegate` and `prefect_dispatch`: pass `directory` on every call so OpenCode edits the right project.
```

(The exact wording can be adjusted while preserving "Always pass directory explicitly" and the list of three tools - prefect_create_session, prefect_delegate, prefect_dispatch.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add global install detection + two-mode PREFECT_ENTRY in src/cli.ts</name>
  <files>src/cli.ts</files>
  <read_first>
    - src/cli.ts (current 74 lines - imports, __dirname, mcpServerPath, PREFECT_ENTRY)
    - package.json (verify Plan 1 added the second bin entry "prefect-mcp": "./build/index.js")
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (section "src/cli.ts" - target pattern)
    - .planning/phases/09-npm-distribution/09-RESEARCH.md (Pattern 3 + Pattern 4 + assumption A1 about volta)
  </read_first>
  <action>
    Update src/cli.ts to detect global vs local install and emit a different PREFECT_ENTRY in each case (per D-09, D-10, DIST-05).

    Replace the current PREFECT_ENTRY definition block (lines 6-20 in the current file) with the following exact code:

```
// Resolve absolute path to build/index.js (the MCP server) from this CLI's
// own location. Both files live side-by-side in the build/ output dir.
const __dirname = dirname(fileURLToPath(import.meta.url));

// DIST-05: detect global vs local install via path-segment check.
// Node resolves symlinks before computing import.meta.url, so __dirname is
// always the real file path inside node_modules/<pkg>/build/ for global installs.
// Normalize backslashes for Windows path support.
const isGlobal = __dirname.replace(/\\/g, '/').includes('/node_modules/prefect-mcp/');

// Template for the prefect entry written into .mcp.json mcpServers.prefect.
// Global: use the prefect-mcp PATH bin (added as a second bin entry in package.json).
// Local: use node + absolute path so Claude Code can spawn from any cwd.
const PREFECT_ENTRY = isGlobal
  ? {
      type: 'stdio',
      command: 'prefect-mcp',
      args: [],
      env: {},
    } as const
  : {
      type: 'stdio',
      command: 'node',
      args: [resolve(__dirname, 'index.js')],
      env: {},
    } as const;
```

    Notes for the executor:
    - Imports at the top of the file (`readFileSync`, `writeFileSync`, `existsSync`, `resolve`, `dirname`, `fileURLToPath`) are unchanged - all symbols needed are already imported.
    - Remove the now-unused `mcpServerPath` const that previously held `resolve(__dirname, 'index.js')`.
    - The two-mode `as const` typing must be applied to each branch object (not the outer ternary) so TypeScript narrows the literal types correctly. If TS complains about type assignability between branches, hoist the discriminator: `const PREFECT_ENTRY = (isGlobal ? globalEntry : localEntry) as const` is acceptable as a fallback.
    - The rest of cli.ts (argv parsing, .mcp.json read/merge/write, force-flag handling) is unchanged.

    Acceptance fallback (per D-11): if global detection proves unreliable in any post-task smoke test, document a `--global` flag that forces global mode. This phase does NOT need to add the flag preemptively - the path-segment approach is verified reliable in RESEARCH.md across all common version managers. Only add the flag if the post-task smoke test fails.

    Smoke test (manual) after the change:
    - Run `npm run build`
    - Run `node build/cli.js init` from a fresh tmp dir; the resulting .mcp.json should have `"command": "node"` (local mode - the cli is being run from the dev checkout).
    - Optional: simulate a global install by symlinking `build/cli.js` into a path containing `/node_modules/prefect-mcp/` and rerun - should emit `"command": "prefect-mcp"`.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && npm run build 2>&amp;1 | tail -10 && grep -q "isGlobal" src/cli.ts && grep -q "/node_modules/prefect-mcp/" src/cli.ts && grep -q "command: 'prefect-mcp'" src/cli.ts && (cd $(mktemp -d) && node /mnt/c/Users/larry/Documents/repos/personal/supervisor/build/cli.js init && grep -q '"command": "node"' .mcp.json && grep -q '"args"' .mcp.json) && npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isGlobal" src/cli.ts` returns at least 2 (declaration + use in ternary)
    - `grep -c "/node_modules/prefect-mcp/" src/cli.ts` returns at least 1 (path-segment literal)
    - `grep -c "replace(/\\\\\\\\/g, '/')" src/cli.ts` returns at least 1 (Windows normalization)
    - `grep -c "command: 'prefect-mcp'" src/cli.ts` returns 1 (global branch command)
    - `grep -c "command: 'node'" src/cli.ts` returns 1 (local branch command)
    - `grep -c "mcpServerPath" src/cli.ts` returns 0 (intermediate var removed)
    - `npm run build` exits 0
    - Running `node build/cli.js init` from a fresh tmp dir produces a `.mcp.json` with `"command": "node"` (since the dev checkout is not in `node_modules/prefect-mcp/`)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>cli.ts has path-segment global detection. PREFECT_ENTRY is conditional. Local-mode smoke test passes. Tests still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CLAUDE.md and examples/test-task.md to prefect_* names + add directory-arg instruction</name>
  <files>CLAUDE.md, examples/test-task.md</files>
  <read_first>
    - CLAUDE.md (current 118 lines - has Canonical Loop, Tool Reference table, env var section)
    - examples/test-task.md (current 57 lines - validation prompt)
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (Documentation section)
    - .planning/phases/09-npm-distribution/09-CONTEXT.md (D-01, D-11)
  </read_first>
  <action>
    Update both documentation files. Two distinct edit types: (a) blanket rename, (b) add the directory-arg instruction (CLAUDE.md only).

    File 1 - CLAUDE.md (~17 tool name occurrences + env var occurrences + DIST-11 instruction add):

    (a) Apply blanket replacements:
    - Tool names: `opencode_` -> `prefect_` (lowercase) anywhere in the file. Affects the Tool Reference table, Canonical Loop steps, callouts, and any inline references. The exact 25-tool mapping in `<interfaces>` applies.
    - Env vars: `OPENCODE_URL` -> `PREFECT_SERVER_URL`, `OPENCODE_SERVER_PASSWORD` -> `PREFECT_SERVER_PASSWORD`, `OPENCODE_SERVER_USERNAME` -> `PREFECT_SERVER_USERNAME`, `OPENCODE_DEFAULT_PROJECT` -> `PREFECT_DEFAULT_PROJECT`. `PREFECT_TIMEOUT_MS` and `PREFECT_AUTOSTART_TIMEOUT_MS` are unchanged. The Environment section may also reference both old and new names with a deprecation note - your call.

    (b) DIST-11 - add the explicit-directory-arg instruction to step 1 of the Canonical Loop. Update the existing step 1 text from:
    `1. **CREATE SESSION.** Call \`opencode_create_session({title: "<short task name>"})\`. Save the returned \`id\` as your session ID for the rest of the loop. If OpenCode was not started from this project's root, also pass \`directory: process.cwd()\` (the absolute path) so the session writes files in the right place.`
    To:
    `1. **CREATE SESSION.** Call \`prefect_create_session({title: "<short task name>", directory: "<absolute path to your project>"})\`. Save the returned \`id\` as your session ID for the rest of the loop. **Always pass \`directory\` explicitly** - never rely on the server's default working directory. The same applies to \`prefect_delegate\` and \`prefect_dispatch\`: pass \`directory\` on every call so OpenCode edits the right project.`

    Verify after edits:
    - `grep -n "opencode_" CLAUDE.md` returns zero matches
    - `grep -n "OPENCODE_URL\|OPENCODE_SERVER_PASSWORD\|OPENCODE_SERVER_USERNAME\|OPENCODE_DEFAULT_PROJECT" CLAUDE.md` matches at most prose mentions in a deprecation note (not canonical instructions)
    - `grep -in "always pass" CLAUDE.md` matches the new instruction line
    - `grep -in "directory" CLAUDE.md` matches the canonical loop step 1

    File 2 - examples/test-task.md (8 tool name occurrences):

    Apply blanket `opencode_` -> `prefect_` replacement. The validation prompt is a step-by-step instruction that calls `opencode_create_session`, `opencode_run`, `opencode_get_diff` (and possibly others); after replacement these all read `prefect_*`. Do not change the prompt's logic, just the tool name strings.

    Verify after edits:
    - `grep -n "opencode_" examples/test-task.md` returns zero matches
    - `grep -c "prefect_create_session" examples/test-task.md` returns at least 1
    - `grep -c "prefect_run" examples/test-task.md` returns at least 1
    - `grep -c "prefect_get_diff" examples/test-task.md` returns at least 1

    Do NOT touch in this task:
    - README.md (Task 3 owns it - includes a separate global-install section addition)
    - .mcp.json templates anywhere in the docs (those reference server names like "prefect", not tool names)
    - Beads/bd command examples (bd is a separate tool, not opencode/prefect)
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && (grep -n "opencode_" CLAUDE.md examples/test-task.md && exit 1 || exit 0) && grep -q "prefect_create_session" CLAUDE.md && grep -q "Always pass" CLAUDE.md && grep -q "prefect_delegate" CLAUDE.md && grep -q "prefect_dispatch" CLAUDE.md && grep -q "PREFECT_SERVER_URL\|PREFECT_SERVER_PASSWORD" CLAUDE.md && grep -q "prefect_create_session" examples/test-task.md && grep -q "prefect_run" examples/test-task.md && grep -q "prefect_get_diff" examples/test-task.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opencode_" CLAUDE.md` returns zero matches (exit 1)
    - `grep -n "opencode_" examples/test-task.md` returns zero matches (exit 1)
    - `grep -c "prefect_create_session" CLAUDE.md` returns at least 2 (Tool Reference + Canonical Loop step 1)
    - `grep -c "prefect_run" CLAUDE.md` returns at least 2
    - `grep -c "prefect_delegate" CLAUDE.md` returns at least 1
    - `grep -c "prefect_dispatch" CLAUDE.md` returns at least 1
    - `grep -c "Always pass" CLAUDE.md` returns at least 1 (DIST-11 directory instruction)
    - `grep -in "directory" CLAUDE.md | grep -i "always" | wc -l` returns at least 1 (the instruction line)
    - `grep -c "PREFECT_SERVER_URL" CLAUDE.md` returns at least 1
    - `grep -c "PREFECT_SERVER_PASSWORD" CLAUDE.md` returns at least 1 (env var section)
    - `grep -c "prefect_create_session" examples/test-task.md` returns at least 1
    - `grep -c "prefect_run" examples/test-task.md` returns at least 1
    - `grep -c "prefect_get_diff" examples/test-task.md` returns at least 1
  </acceptance_criteria>
  <done>CLAUDE.md uses prefect_* names everywhere, includes the DIST-11 explicit-directory-arg instruction, and references PREFECT_* env vars. examples/test-task.md uses prefect_* names. Zero opencode_ references remain in either file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md - rename + add global install pathway + verify npm pack</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 205 lines - has install section, env var section, tool reference)
    - package.json (verify Plan 1 fields: name=prefect-mcp, files, bin entries)
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (Documentation section - README global install pathway)
    - .planning/phases/09-npm-distribution/09-RESEARCH.md (Pitfall 5 - test files in npm pack output are acceptable)
  </read_first>
  <action>
    Update README.md in three steps. After all edits, run `npm pack --dry-run` to verify DIST-04.

    Step 1 - Apply blanket renames:
    - Tool names: `opencode_` -> `prefect_` (lowercase) - 26 occurrences expected per RESEARCH.md inventory.
    - Env vars: `OPENCODE_URL` -> `PREFECT_SERVER_URL`, `OPENCODE_SERVER_PASSWORD` -> `PREFECT_SERVER_PASSWORD`, `OPENCODE_SERVER_USERNAME` -> `PREFECT_SERVER_USERNAME`, `OPENCODE_DEFAULT_PROJECT` -> `PREFECT_DEFAULT_PROJECT`. Add a brief note in the env var table: "Old `OPENCODE_*` names still work but emit a stderr deprecation warning - migrate to `PREFECT_*` names. Old names will be removed in v4.0." (per D-04, D-05, D-06).
    - Preserve the existing INFRA-06 warning ("never put `PREFECT_SERVER_PASSWORD` in `.mcp.json` env block") - just rotate the env var name in it.

    Step 2 - Add global install pathway section (DIST-06):
    Currently README has a single install pathway (clone + build local). Add a new top-level "Install" section near the top of the file (before any existing setup instructions) with both pathways. Use the snippet template in the `<interfaces>` block as a starting point, adapting headings/wording to match README.md's existing style.

    Required content:
    - Heading describing global vs local
    - `npm install -g prefect-mcp` command for global
    - `prefect init` step that auto-detects mode
    - Example .mcp.json snippet for global mode (command: prefect-mcp, args: [])
    - Example .mcp.json snippet for local mode (command: node, args: [/abs/path/to/build/index.js])
    - Note that `prefect init --force` overwrites an existing prefect entry

    If the existing README has a "Setup" or "Quickstart" section, replace its install-related content rather than duplicating. The structural goal: a reader landing on the README sees the global-install option first (recommended), with the local option preserved for contributors.

    Step 3 - npm pack --dry-run verification (DIST-04):
    After all README edits and `npm run build`, run:

```
npm pack --dry-run 2>&1
```

    PASS criteria (DIST-04, accounting for Pitfall 5):
    - Output lists files under `build/` (including `build/*.js` and acceptably `build/*.test.js` per Pitfall 5)
    - Output lists `README.md`
    - Output does NOT contain `src/` (no TypeScript sources)
    - Output does NOT contain `node_modules/`
    - Output does NOT contain `.planning/`
    - Output does NOT contain `.git/`
    - Output does NOT contain `.mcp.json` (project-level config, not part of the package)

    Capture the dry-run output and embed it in the SUMMARY.md (Output section) so the publish step has a verified record.

    Do NOT touch in this task:
    - .npmignore (we use `files` whitelist; .npmignore is not present and not needed)
    - package.json (Plan 1 owns it; this task only reads it for context)
    - Tool description files inside src/ (Plan 1 owns them)
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && (grep -n "opencode_" README.md && exit 1 || exit 0) && grep -q "npm install -g prefect-mcp" README.md && grep -q "command.*prefect-mcp" README.md && grep -q "PREFECT_SERVER_URL" README.md && grep -q "PREFECT_SERVER_PASSWORD" README.md && grep -q "deprecat" README.md && npm run build 2>&amp;1 | tail -5 && PACK=$(npm pack --dry-run 2>&amp;1) && echo "$PACK" | grep -q "build/" && echo "$PACK" | grep -q "README.md" && (echo "$PACK" | grep -E "(^|/)src/" && exit 1 || exit 0) && (echo "$PACK" | grep -q "node_modules" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opencode_" README.md` returns zero matches (exit 1)
    - `grep -c "prefect_create_session" README.md` returns at least 1
    - `grep -c "prefect_run" README.md` returns at least 1
    - `grep -c "npm install -g prefect-mcp" README.md` returns at least 1 (DIST-06 global pathway)
    - `grep -c "prefect-mcp" README.md` returns at least 2 (npm install command + .mcp.json snippet)
    - `grep -c "command.*node" README.md` returns at least 1 (local pathway snippet)
    - `grep -c "PREFECT_SERVER_URL" README.md` returns at least 1
    - `grep -c "PREFECT_SERVER_PASSWORD" README.md` returns at least 1
    - `grep -c "PREFECT_SERVER_USERNAME" README.md` returns at least 1
    - `grep -c "PREFECT_DEFAULT_PROJECT" README.md` returns at least 1
    - `grep -ci "deprecat" README.md` returns at least 1 (deprecation note about OPENCODE_*)
    - `npm pack --dry-run 2>&1` output contains `build/` and `README.md` lines
    - `npm pack --dry-run 2>&1` output does NOT contain `src/index.ts`, `src/auth.ts`, or any other `.ts` source path
    - `npm pack --dry-run 2>&1` output does NOT contain `node_modules`
    - `npm pack --dry-run 2>&1` output does NOT contain `.planning`
    - `npm pack --dry-run 2>&1` output does NOT contain `.mcp.json`
  </acceptance_criteria>
  <done>README.md uses prefect_* names, documents both install pathways (global + local), uses PREFECT_* env vars with deprecation note, and `npm pack --dry-run` confirms the tarball ships only build/ + README.md (DIST-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem path -> isGlobal classifier | A user with a project in a path containing `/node_modules/prefect-mcp/` (e.g. a developer working on prefect-mcp checked out at that exact path) could trigger global mode incorrectly. Edge case. |
| Documentation prose -> user behavior | README and CLAUDE.md instructions become the authoritative source for how callers use the package. Misleading docs cause runtime errors. |
| npm pack tarball -> public registry | Final pack output determines what ships. A misconfiguration here could publish secrets or src/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | T (Tampering) | isGlobal classifier false positive on developer checkout | accept | A developer's prefect-mcp checkout at a literal path containing `/node_modules/prefect-mcp/` would trigger global mode. Extremely unusual for a dev checkout. D-11 fallback: a `--global` flag could be added to force the mode if this becomes an issue. Smoke test in Task 1 catches the common case (dev runs from local checkout - returns local). |
| T-09-07 | I (Information disclosure) | npm pack output | mitigate | Task 3 verifies via `npm pack --dry-run` that only build/ + README.md ship. Acceptance criteria explicitly check absence of src/, node_modules/, .planning/, .mcp.json. The `files` whitelist (set by Plan 1) is the primary control. |
| T-09-08 | I (Information disclosure) | README env var instructions | mitigate | DIST-06 README warns users not to put PREFECT_SERVER_PASSWORD in .mcp.json (preserves existing INFRA-06 warning). Soft-migration warning surfaces deprecated OPENCODE_* usage to stderr so users notice and migrate. |
| T-09-09 | T (Tampering) | CLAUDE.md canonical loop directory arg | mitigate | DIST-11 instruction added to step 1: callers always pass `directory` explicitly. Prevents the failure mode where OpenCode edits the wrong project because the server cwd differs from the caller's intent. |
| T-09-10 | E (Elevation of privilege) | `npm install -g prefect-mcp` | accept | Standard npm global-install pathway. Same trust model as any global npm bin. README does not introduce any post-install scripts; package has no `postinstall` hook. The bin scripts (cli.js, index.js) only do what the local-checkout binaries do. |

Security rationale: Plan 2 is documentation + cli.ts wiring. The cli.ts change is mode-detection logic only - it does not change what code runs, just which `command` string `prefect init` writes into .mcp.json. README and CLAUDE.md changes are user-facing prose. The npm pack verification is the primary mitigation against publishing source files or secrets.
</threat_model>

<verification>
After all tasks complete:

1. `grep -rn "opencode_" CLAUDE.md README.md examples/test-task.md` returns zero matches (DIST-07 docs portion satisfied)
2. `grep -rn "opencode_" src/cli.ts` returns zero matches (no leftover from Plan 1)
3. CLAUDE.md canonical loop step 1 contains "Always pass `directory`" instruction (DIST-11)
4. README.md contains `npm install -g prefect-mcp` and an example .mcp.json snippet for both modes (DIST-06)
5. examples/test-task.md uses prefect_* tool names throughout (DIST-10)
6. CLAUDE.md uses prefect_* tool names in Tool Reference table and Canonical Loop (DIST-09)
7. All four env vars renamed across docs: PREFECT_SERVER_URL, PREFECT_SERVER_PASSWORD, PREFECT_SERVER_USERNAME, PREFECT_DEFAULT_PROJECT (DIST-12 docs portion satisfied)
8. src/cli.ts has isGlobal detection and two-mode PREFECT_ENTRY (DIST-05)
9. `npm pack --dry-run` shows only build/ + README.md, no src/ or node_modules/ (DIST-04)
10. `npm test` still exits 0 after Task 1 cli.ts changes
</verification>

<success_criteria>
- src/cli.ts uses path-segment global detection and emits the correct PREFECT_ENTRY for both modes
- CLAUDE.md uses prefect_* tool names and includes the DIST-11 explicit-directory-arg instruction
- README.md documents both install pathways (global + local) with .mcp.json examples
- examples/test-task.md uses prefect_* tool names
- All env var references in docs use PREFECT_* canonical names with a note about deprecated OPENCODE_* fallback
- `npm pack --dry-run` verified - tarball ships only build/ and README.md (DIST-04)
- `npm test` still exits 0 (cli.ts changes do not break tests)
- Zero opencode_ references in any of CLAUDE.md, README.md, examples/test-task.md, src/cli.ts
- Phase 9 is ready for the manual `npm publish` step (out of scope for this phase)
</success_criteria>

<output>
After completion, create `.planning/phases/09-npm-distribution/09-02-SUMMARY.md` capturing:
- Files modified (count and names)
- npm pack --dry-run output (full text) - the verified DIST-04 evidence
- Tool name + env var occurrence counts before/after in each doc file
- Smoke-test result: `node build/cli.js init` from tmp dir produces local-mode .mcp.json
- Confirmation that the package is ready for `npm publish` as a manual step
- Any deviations from the plan (e.g. README structural reorganization, prose adjustments)
</output>
