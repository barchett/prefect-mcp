---
phase: 09-npm-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/auth.ts
  - src/config.ts
  - src/autostart.ts
  - src/handlers.ts
  - src/auth.test.ts
  - src/autostart.test.ts
  - src/diff-patch.test.ts
  - src/session-command.test.ts
  - package.json
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-07
  - DIST-08
  - DIST-12
tags:
  - npm-publishing
  - tool-rename
  - env-var-migration

must_haves:
  truths:
    - "All opencode_* tool name string literals in src/*.ts are renamed to prefect_*"
    - "All canonical OPENCODE_* env var reads in src/ become PREFECT_* primary reads with soft-migration fallback to old names"
    - "Reading an old OPENCODE_* env var emits a one-time stderr deprecation warning per read site"
    - "PREFECT_TIMEOUT_MS and PREFECT_AUTOSTART_TIMEOUT_MS are unchanged"
    - "Test files set canonical new env var names (PREFECT_SERVER_PASSWORD, PREFECT_SERVER_URL, PREFECT_SERVER_USERNAME) - not OPENCODE_* names"
    - "package.json contains name=prefect-mcp, description, license=MIT, engines node>=20, files=[build/, README.md], publishConfig access=public, second bin entry prefect-mcp -> ./build/index.js"
    - "npm run build exits 0"
    - "npm test exits 0 after the rename"
    - "grep -rn 'opencode_' src/ returns zero matches"
  artifacts:
    - path: "src/index.ts"
      provides: "All 25 tool registrations renamed to prefect_*; BASE_URL reads PREFECT_SERVER_URL with OPENCODE_URL fallback"
      contains: "prefect_create_session"
    - path: "src/auth.ts"
      provides: "buildAuthHeader reads PREFECT_SERVER_PASSWORD/USERNAME with one-time-warned fallback"
      contains: "PREFECT_SERVER_PASSWORD"
    - path: "src/config.ts"
      provides: "resolveDirectory reads PREFECT_DEFAULT_PROJECT with one-time-warned fallback; still returns undefined when no value"
      contains: "PREFECT_DEFAULT_PROJECT"
    - path: "src/autostart.ts"
      provides: "BASE_URL reads PREFECT_SERVER_URL with OPENCODE_URL fallback; remote-host error uses PREFECT_SERVER_URL"
      contains: "PREFECT_SERVER_URL"
    - path: "package.json"
      provides: "npm publishing manifest with prefect-mcp name, files whitelist, dual bin entries"
      contains: "prefect-mcp"
  key_links:
    - from: "src/index.ts tool registrations"
      to: "all 25 prefect_* tool names"
      via: "blanket opencode_ -> prefect_ replacement"
      pattern: "prefect_(create_session|run|delegate|dispatch|inspect|await|find_symbol|list_agents|list_providers)"
    - from: "src/auth.ts buildAuthHeader"
      to: "process.env.PREFECT_SERVER_PASSWORD"
      via: "primary read with module-level warnedPassword guard for OPENCODE_SERVER_PASSWORD fallback"
      pattern: "process\\.env\\.PREFECT_SERVER_PASSWORD"
    - from: "package.json bin"
      to: "build/index.js"
      via: "second bin key prefect-mcp"
      pattern: "\"prefect-mcp\":\\s*\"\\./build/index\\.js\""
---

<objective>
Rename all opencode_* tool name string literals to prefect_* across every src/*.ts file, migrate all four OPENCODE_* env var reads to PREFECT_* with a one-time-warned soft-migration fallback (per D-04, D-05, D-07), update test files to use the canonical new env var names (per D-03), and add npm publishing fields to package.json (per D-12 through D-18). Verify by running npm run build and npm test to a clean exit (per D-02).

Purpose: Land the entire mechanical rename + publishing manifest in a single isolated wave with the test gate proving correctness, before any documentation work begins (per D-02). Plan 2 then handles docs, the cli.ts global-install detection work, and npm pack verification.

Output: Renamed source + tests, new publishing manifest, build artifacts that pass npm test. No docs changes (those land in Plan 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-npm-distribution/09-CONTEXT.md
@.planning/phases/09-npm-distribution/09-RESEARCH.md
@.planning/phases/09-npm-distribution/09-PATTERNS.md
@CLAUDE.md
@package.json
@src/index.ts
@src/auth.ts
@src/config.ts
@src/autostart.ts
@src/handlers.ts
@src/auth.test.ts
@src/autostart.test.ts

<interfaces>
<!-- Soft migration patterns extracted from RESEARCH.md (runtime-verified). -->
<!-- Executor uses these exactly - do not redesign. -->

Pattern 1 - module-init env read (no warned flag, fires once at module load):

```
const BASE_URL =
  process.env.PREFECT_SERVER_URL ??
  (() => {
    const old = process.env.OPENCODE_URL;
    if (old) console.error('[Prefect] OPENCODE_URL is deprecated, use PREFECT_SERVER_URL');
    return old;
  })() ??
  'http://localhost:4096';
```

Pattern 2 - call-time env read (module-level warned flag, fires at most once per process):

```
let warnedDefaultProject = false;

export function resolveDirectory(perToolParam) {
  return (
    perToolParam ??
    process.env.PREFECT_DEFAULT_PROJECT ??
    (() => {
      const old = process.env.OPENCODE_DEFAULT_PROJECT;
      if (old && !warnedDefaultProject) {
        console.error('[Prefect] OPENCODE_DEFAULT_PROJECT is deprecated, use PREFECT_DEFAULT_PROJECT');
        warnedDefaultProject = true;
      }
      return old;
    })()
  );
}
```

Read-time classification (which files use which pattern):

| File | Var | Pattern |
|------|-----|---------|
| src/index.ts | OPENCODE_URL | 1 (module-init) |
| src/autostart.ts | OPENCODE_URL | 1 (module-init) |
| src/config.ts | OPENCODE_DEFAULT_PROJECT | 2 (call-time, warned flag) |
| src/auth.ts | OPENCODE_SERVER_PASSWORD | 2 (call-time, warned flag) |
| src/auth.ts | OPENCODE_SERVER_USERNAME | 2 (call-time, warned flag) |

Tool rename mapping (all 25, exact strings):

```
opencode_create_session    -> prefect_create_session
opencode_abort             -> prefect_abort
opencode_run               -> prefect_run
opencode_prompt_async      -> prefect_prompt_async
opencode_get_diff          -> prefect_get_diff
opencode_approve_permission-> prefect_approve_permission
opencode_fork              -> prefect_fork
opencode_revert            -> prefect_revert
opencode_session_list      -> prefect_session_list
opencode_session_get       -> prefect_session_get
opencode_session_status    -> prefect_session_status
opencode_session_messages  -> prefect_session_messages
opencode_session_message   -> prefect_session_message
opencode_session_delete    -> prefect_session_delete
opencode_session_rename    -> prefect_session_rename
opencode_session_children  -> prefect_session_children
opencode_session_unrevert  -> prefect_session_unrevert
opencode_session_command   -> prefect_session_command
opencode_delegate          -> prefect_delegate
opencode_dispatch          -> prefect_dispatch
opencode_inspect           -> prefect_inspect
opencode_await             -> prefect_await
opencode_list_agents       -> prefect_list_agents
opencode_list_providers    -> prefect_list_providers
opencode_find_symbol       -> prefect_find_symbol
```

Env var rename mapping (D-07, exact):

```
OPENCODE_URL              -> PREFECT_SERVER_URL
OPENCODE_SERVER_PASSWORD  -> PREFECT_SERVER_PASSWORD
OPENCODE_SERVER_USERNAME  -> PREFECT_SERVER_USERNAME
OPENCODE_DEFAULT_PROJECT  -> PREFECT_DEFAULT_PROJECT
PREFECT_TIMEOUT_MS        -> unchanged
PREFECT_AUTOSTART_TIMEOUT_MS -> unchanged
```

package.json target shape:

```
{
  "name": "prefect-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "TypeScript MCP server that exposes OpenCode's HTTP API as Claude Code tools",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "files": ["build/", "README.md"],
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js build/cli.js",
    "test": "tsc && node --test build/*.test.js"
  },
  "bin": {
    "prefect": "./build/cli.js",
    "prefect-mcp": "./build/index.js"
  },
  "dependencies": { ...unchanged... },
  "devDependencies": { ...unchanged... }
}
```

Field ordering: prefer placing the new top-level fields (`name`, `version`, `type`, `description`, `license`, `engines`, `files`, `publishConfig`) in the order shown above near the top of the file; existing `scripts`, `bin`, `dependencies`, `devDependencies` blocks remain. JSON key ordering is not strictly enforced by npm but the shape above is the canonical npm convention.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename all opencode_* tool names to prefect_* across src/*.ts</name>
  <files>src/index.ts, src/handlers.ts, src/diff-patch.test.ts, src/session-command.test.ts</files>
  <read_first>
    - src/index.ts (current state of all 25 tool registrations and description strings)
    - src/handlers.ts (4 comment references to opencode_* names)
    - src/diff-patch.test.ts (1 comment reference)
    - src/session-command.test.ts (1 comment reference)
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (rename inventory + Pitfall 1)
    - .planning/phases/09-npm-distribution/09-RESEARCH.md (Tool Names to Rename - full 25-tool list)
  </read_first>
  <action>
    Perform a blanket `opencode_` -> `prefect_` (lowercase prefix only) replacement in these four files using the Edit tool. Verify the exact 25-tool mapping in the `<interfaces>` block - no tool name should remain with the `opencode_` prefix.

    Scope per file:
    - `src/index.ts` (~80 occurrences): touches (1) tool name string literals like `server.registerTool('opencode_create_session', ...)`, (2) inputSchema `.describe()` strings like `'Session ID returned from opencode_create_session'`, (3) tool description fields, (4) error message strings. Apply the blanket replacement across all four contexts.
    - `src/handlers.ts` (4 occurrences in JSDoc comments): `opencode_create_session handler`, `opencode_run handler`, `composite handlers (opencode_delegate)`, `opencode_get_diff handler` - update to `prefect_*` equivalents.
    - `src/diff-patch.test.ts` (1 occurrence in a comment): `the opencode_get_diff handler` -> `the prefect_get_diff handler`.
    - `src/session-command.test.ts` (1 occurrence in a comment): `opencode_session_command input schema` -> `prefect_session_command input schema`.

    Do NOT touch in this task:
    - `src/cli.ts` (Plan 2 owns it)
    - Any `OPENCODE_*` env var references (Task 2 owns those - they have uppercase OPENCODE prefix and are distinguishable from tool names)
    - Any `*.md` files (Plan 2 owns docs)
    - `package.json` (Task 4 owns it)
    - `src/auth.ts`, `src/config.ts`, `src/autostart.ts` (no opencode_* tool name occurrences in those files; Task 2 owns env var migration there)

    Verification gate after this task: `grep -rn "opencode_" src/ --include='*.ts'` must return zero matches and `npm run build` must exit 0.

    Pitfall 1 from RESEARCH.md: a blanket replacement on the lowercase prefix `opencode_` matches both tool name literals and description prose - this is intentional. After replacement, run grep to confirm zero residual matches.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && npm run build 2>&amp;1 | tail -20 && (grep -rn "opencode_" src/ --include='*.ts' && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "opencode_" src/ --include='*.ts'` exits with status 1 (zero matches)
    - `grep -c "prefect_create_session" src/index.ts` returns at least 2
    - `grep -c "prefect_run" src/index.ts` returns at least 2
    - `grep -c "prefect_delegate" src/index.ts` returns at least 1
    - `grep -c "prefect_dispatch" src/index.ts` returns at least 1
    - `grep -c "prefect_inspect" src/index.ts` returns at least 1
    - `grep -c "prefect_await" src/index.ts` returns at least 1
    - `grep -c "prefect_find_symbol" src/index.ts` returns at least 1
    - `grep -c "prefect_list_agents" src/index.ts` returns at least 1
    - `grep -c "prefect_list_providers" src/index.ts` returns at least 1
    - `grep -n "prefect_get_diff handler" src/handlers.ts` returns a match
    - `grep -n "prefect_get_diff" src/diff-patch.test.ts` returns a match
    - `grep -n "prefect_session_command" src/session-command.test.ts` returns a match
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 25 tool names renamed in src/*.ts. Zero opencode_ references remain in src/. Build succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply env var soft migration in 4 source files</name>
  <files>src/index.ts, src/autostart.ts, src/auth.ts, src/config.ts</files>
  <read_first>
    - src/index.ts (current BASE_URL read at top of file - module-init pattern)
    - src/autostart.ts (current BASE_URL read line 7 + remote-host error string lines 78-79 + JSDoc references on lines 64, 66, 72)
    - src/auth.ts (current buildAuthHeader call-time reads, lines 12-18)
    - src/config.ts (current resolveDirectory call-time read, lines 13-15)
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (Pattern assignments per file + Module-Init vs Call-Time table)
    - .planning/phases/09-npm-distribution/09-RESEARCH.md (Pattern 1 module-init, Pattern 2 call-time, Code Examples - auth.ts)
  </read_first>
  <action>
    Replace the 4 canonical OPENCODE_* env var reads with PREFECT_* primary reads + soft-migration fallback. Apply Pattern 1 (module-init) or Pattern 2 (call-time) per file as documented in `<interfaces>` and 09-PATTERNS.md. All warnings MUST go to console.error (stdout is the JSON-RPC pipe).

    File 1 - src/index.ts (Pattern 1, module-init):
    Find the existing `const BASE_URL = process.env.OPENCODE_URL ?? 'http://localhost:4096';` line near the top of the file. Replace it with the Pattern 1 IIFE form (see `<interfaces>` Pattern 1 - exact code). Leave `TIMEOUT_MS` (already reads `PREFECT_TIMEOUT_MS`) unchanged. If a CORE-08 comment references OPENCODE_URL, update it to reference PREFECT_SERVER_URL.

    File 2 - src/autostart.ts (Pattern 1, module-init):
    Replace `const BASE_URL = process.env.OPENCODE_URL ?? 'http://localhost:4096';` (line 7) with the same Pattern 1 IIFE. Then update prose references in this file:
    - JSDoc on line 64: `OPENCODE_DEFAULT_PROJECT if set` -> `PREFECT_DEFAULT_PROJECT if set`
    - JSDoc on line 66: `Port parsed from OPENCODE_URL` -> `Port parsed from PREFECT_SERVER_URL`
    - Comment line 72: `Fail fast if OPENCODE_URL points to a remote host` -> `Fail fast if PREFECT_SERVER_URL points to a remote host`
    - Error string line 78: `Auto-start skipped — OPENCODE_URL points to remote host '${hostname}'.` -> `Auto-start skipped — PREFECT_SERVER_URL points to remote host '${hostname}'.`

    File 3 - src/auth.ts (Pattern 2, call-time, two warned flags):
    Replace the entire buildAuthHeader function with the call-time soft-migration version. Add two module-level flags at the top of the file (above the existing JSDoc comment block):

```
let warnedPassword = false;
let warnedUsername = false;

export function buildAuthHeader() {
  const password =
    process.env.PREFECT_SERVER_PASSWORD ??
    (() => {
      const old = process.env.OPENCODE_SERVER_PASSWORD;
      if (old && !warnedPassword) {
        console.error('[Prefect] OPENCODE_SERVER_PASSWORD is deprecated, use PREFECT_SERVER_PASSWORD');
        warnedPassword = true;
      }
      return old;
    })();

  if (!password) return {};

  const username =
    process.env.PREFECT_SERVER_USERNAME ??
    (() => {
      const old = process.env.OPENCODE_SERVER_USERNAME;
      if (old && !warnedUsername) {
        console.error('[Prefect] OPENCODE_SERVER_USERNAME is deprecated, use PREFECT_SERVER_USERNAME');
        warnedUsername = true;
      }
      return old;
    })() ??
    'opencode';

  const token = Buffer.from(`${username}:${password}`).toString('base64');
  return { Authorization: `Basic ${token}` };
}
```

    Use the actual TypeScript signature from the existing file: `export function buildAuthHeader(): Record<string, string>` and preserve the JSDoc above it. Update the JSDoc text to reference PREFECT_SERVER_PASSWORD and PREFECT_SERVER_USERNAME (with note about old-name fallback).

    File 4 - src/config.ts (Pattern 2, call-time, one warned flag):
    Replace the existing 3-line resolveDirectory function. Add `warnedDefaultProject` flag at module level above the JSDoc:

```
let warnedDefaultProject = false;

export function resolveDirectory(perToolParam: string | undefined): string | undefined {
  return (
    perToolParam ??
    process.env.PREFECT_DEFAULT_PROJECT ??
    (() => {
      const old = process.env.OPENCODE_DEFAULT_PROJECT;
      if (old && !warnedDefaultProject) {
        console.error('[Prefect] OPENCODE_DEFAULT_PROJECT is deprecated, use PREFECT_DEFAULT_PROJECT');
        warnedDefaultProject = true;
      }
      return old;
    })()
  );
}
```

    CRITICAL preservation: the function MUST still return undefined when neither param nor either env var is set (per Phase 5 locked design decision - never silently override with process.cwd()). The pattern above preserves this - when both env vars are unset, the IIFE returns undefined and the `??` chain bubbles undefined out.

    Update the JSDoc above the function: `OPENCODE_DEFAULT_PROJECT env var` -> `PREFECT_DEFAULT_PROJECT env var (with OPENCODE_DEFAULT_PROJECT fallback)`. Preserve the comment that documents the no-cwd fallback contract.

    Do NOT touch in this task:
    - `PREFECT_TIMEOUT_MS` reads (already correct prefix)
    - `PREFECT_AUTOSTART_TIMEOUT_MS` reads (already correct prefix)
    - Test files (Task 3 owns them)
    - Tool name strings (Task 1 owns those)
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && npm run build 2>&amp;1 | tail -20 && grep -q "PREFECT_SERVER_URL" src/index.ts && grep -q "PREFECT_SERVER_URL" src/autostart.ts && grep -q "PREFECT_SERVER_PASSWORD" src/auth.ts && grep -q "PREFECT_SERVER_USERNAME" src/auth.ts && grep -q "PREFECT_DEFAULT_PROJECT" src/config.ts && grep -q "warnedPassword" src/auth.ts && grep -q "warnedUsername" src/auth.ts && grep -q "warnedDefaultProject" src/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "process.env.PREFECT_SERVER_URL" src/index.ts` returns at least 1
    - `grep -c "process.env.PREFECT_SERVER_URL" src/autostart.ts` returns at least 1
    - `grep -c "process.env.PREFECT_SERVER_PASSWORD" src/auth.ts` returns at least 1
    - `grep -c "process.env.PREFECT_SERVER_USERNAME" src/auth.ts` returns at least 1
    - `grep -c "process.env.PREFECT_DEFAULT_PROJECT" src/config.ts` returns at least 1
    - `grep -c "let warnedPassword = false" src/auth.ts` returns 1
    - `grep -c "let warnedUsername = false" src/auth.ts` returns 1
    - `grep -c "let warnedDefaultProject = false" src/config.ts` returns 1
    - `grep -c "OPENCODE_URL is deprecated" src/index.ts` returns at least 1 (the deprecation warning string)
    - `grep -c "OPENCODE_URL is deprecated" src/autostart.ts` returns at least 1
    - `grep -c "OPENCODE_SERVER_PASSWORD is deprecated" src/auth.ts` returns at least 1
    - `grep -c "OPENCODE_SERVER_USERNAME is deprecated" src/auth.ts` returns at least 1
    - `grep -c "OPENCODE_DEFAULT_PROJECT is deprecated" src/config.ts` returns at least 1
    - `grep -n "PREFECT_SERVER_URL points to remote host" src/autostart.ts` returns a match
    - `grep -c "console.log" src/auth.ts src/config.ts src/index.ts src/autostart.ts` returns 0 (only console.error allowed)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 4 OPENCODE_* env var canonical reads replaced with PREFECT_* primary + soft-migration fallback. Module-level warned flags present in src/auth.ts and src/config.ts. autostart.ts remote-host error string updated. Build succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update test files to canonical PREFECT_* env var names</name>
  <files>src/auth.test.ts, src/autostart.test.ts</files>
  <read_first>
    - src/auth.test.ts (full file - 24 OPENCODE_SERVER_PASSWORD/USERNAME occurrences)
    - src/autostart.test.ts (full file - 10 occurrences including line ~40 remote-guard test setup)
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (sections "src/auth.test.ts" and "src/autostart.test.ts" + Pitfall 2 + Pitfall 3)
    - .planning/phases/09-npm-distribution/09-RESEARCH.md (Pitfall 2 and Pitfall 3 detail)
  </read_first>
  <action>
    Update the two test files to set canonical new env var names (per D-03). After Task 2, the source code reads PREFECT_* as the primary path; tests must set those names so they exercise the primary read path, not the soft-migration fallback (Pitfall 2).

    File 1 - src/auth.test.ts:
    Replace every occurrence of these tokens (24 total):
    - `OPENCODE_SERVER_PASSWORD` -> `PREFECT_SERVER_PASSWORD`
    - `OPENCODE_SERVER_USERNAME` -> `PREFECT_SERVER_USERNAME`

    Replacement scope includes:
    - `process.env.OPENCODE_SERVER_PASSWORD = '...'` assignments -> `process.env.PREFECT_SERVER_PASSWORD = '...'`
    - `delete process.env.OPENCODE_SERVER_PASSWORD` -> `delete process.env.PREFECT_SERVER_PASSWORD`
    - `const prev = process.env.OPENCODE_SERVER_PASSWORD` -> `const prev = process.env.PREFECT_SERVER_PASSWORD`
    - Test description strings like `'buildAuthHeader returns {} when OPENCODE_SERVER_PASSWORD is not set'` -> `'buildAuthHeader returns {} when PREFECT_SERVER_PASSWORD is not set'`
    - Same treatment for OPENCODE_SERVER_USERNAME -> PREFECT_SERVER_USERNAME

    Use the test save/restore pattern documented in 09-PATTERNS.md ("Test Env Var Save/Restore Pattern") - structure is unchanged, only the variable names rotate.

    File 2 - src/autostart.test.ts:
    Replace every occurrence (10 total):
    - `OPENCODE_URL` -> `PREFECT_SERVER_URL`
    - `OPENCODE_SERVER_PASSWORD` -> `PREFECT_SERVER_PASSWORD`

    CRITICAL (Pitfall 3): the remote-guard test (around line 40-45) uses a fresh module import via `?v=remote-guard-test` cache-bust. After Task 2, autostart.ts reads PREFECT_SERVER_URL at module init. This test must set `process.env.PREFECT_SERVER_URL = 'http://192.168.1.100:4096'` (NOT OPENCODE_URL) before the fresh import - otherwise the module reads undefined, falls through to soft-migration, and silently picks up the still-set OPENCODE_URL fallback or the default 'http://localhost:4096', exercising the wrong code path.

    Replacement scope:
    - `process.env.OPENCODE_URL` -> `process.env.PREFECT_SERVER_URL` (read sites)
    - `process.env.OPENCODE_URL = 'http://...'` -> `process.env.PREFECT_SERVER_URL = 'http://...'`
    - `delete process.env.OPENCODE_URL` -> `delete process.env.PREFECT_SERVER_URL`
    - `const origUrl = process.env.OPENCODE_URL` -> `const origUrl = process.env.PREFECT_SERVER_URL`
    - Same treatment for OPENCODE_SERVER_PASSWORD -> PREFECT_SERVER_PASSWORD

    Do NOT touch in this task:
    - `PREFECT_TIMEOUT_MS` / `PREFECT_AUTOSTART_TIMEOUT_MS` references (already correct)
    - Test logic, assertion values, mock structures (only env var name strings rotate)
    - src/cli.test.ts (no OPENCODE_ refs per RESEARCH.md inventory)

    After replacements, both test files must compile (`npm run build`) and run (`npm test`) green.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && npm run build 2>&amp;1 | tail -20 && npm test 2>&amp;1 | tail -30 && (grep -n "OPENCODE_" src/auth.test.ts src/autostart.test.ts && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "OPENCODE_" src/auth.test.ts` returns zero matches (exit 1)
    - `grep -n "OPENCODE_" src/autostart.test.ts` returns zero matches (exit 1)
    - `grep -c "PREFECT_SERVER_PASSWORD" src/auth.test.ts` returns at least 12 (replaces all 24 OPENCODE_SERVER_* refs roughly evenly)
    - `grep -c "PREFECT_SERVER_USERNAME" src/auth.test.ts` returns at least 6
    - `grep -c "PREFECT_SERVER_URL" src/autostart.test.ts` returns at least 6
    - `grep -n "192.168.1.100" src/autostart.test.ts` shows the assignment uses PREFECT_SERVER_URL (the line setting `process.env.PREFECT_SERVER_URL = 'http://192.168.1.100:4096'` exists)
    - `npm run build` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Both test files use canonical PREFECT_* env var names. Zero OPENCODE_ references remain in either test file. Tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add npm publishing fields to package.json + final test gate</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state - 23 lines, no publishing fields)
    - .planning/phases/09-npm-distribution/09-PATTERNS.md (section "package.json (config) - publishing fields")
    - .planning/phases/09-npm-distribution/09-RESEARCH.md (Pattern 5: package.json Publishing Fields + Pitfall 4 + Pitfall 5)
    - .planning/phases/09-npm-distribution/09-CONTEXT.md (D-12 through D-18)
  </read_first>
  <action>
    Update package.json to the target shape (see `<interfaces>` package.json target shape). Apply these specific edits:

    1. Change `"name": "prefect"` -> `"name": "prefect-mcp"` (D-12, DIST-01).
    2. After `"type": "module"`, add three new fields:
       - `"description": "TypeScript MCP server that exposes OpenCode's HTTP API as Claude Code tools"` (D-18, DIST-03)
       - `"license": "MIT"` (D-14, DIST-03)
       - `"engines": { "node": ">=20" }` (D-16, DIST-03)
    3. After `"engines"`, add:
       - `"files": ["build/", "README.md"]` (D-15, DIST-02)
       - `"publishConfig": { "access": "public" }` (D-17, DIST-03)
    4. Update the `"bin"` block to include a SECOND entry pointing to the MCP server entrypoint:
       `"bin": { "prefect": "./build/cli.js", "prefect-mcp": "./build/index.js" }`
       Rationale: enables `npm install -g prefect-mcp` to expose `prefect-mcp` on PATH so `prefect init` can write `"command": "prefect-mcp"` for global installs (DIST-05). build/index.js already has `#!/usr/bin/env node` shebang and is already chmoded by the existing build script - no build script changes needed (Pitfall 4).
    5. Leave `"version": "1.0.0"` unchanged (D-13).
    6. Leave `"scripts"`, `"dependencies"`, and `"devDependencies"` exactly as they are.

    Final file shape must be valid JSON. Do not introduce trailing commas. Preserve existing 2-space indentation. JSON key ordering is conventional but not strictly enforced - prefer the order shown in `<interfaces>`.

    After writing the file:
    - Run `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` to verify valid JSON.
    - Run `npm run build` to confirm tsc still compiles.
    - Run `npm test` as the final gate (per D-02). The wave does not complete until both build and test pass.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/personal/supervisor && node -e "JSON.parse(require('fs').readFileSync('package.json'))" && grep -q '"name": "prefect-mcp"' package.json && grep -q '"license": "MIT"' package.json && grep -q '"engines"' package.json && grep -q '"files"' package.json && grep -q '"publishConfig"' package.json && grep -q '"prefect-mcp": "./build/index.js"' package.json && npm run build 2>&amp;1 | tail -10 && npm test 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` exits 0 (valid JSON)
    - `grep -c '"name": "prefect-mcp"' package.json` returns 1
    - `grep -c '"description"' package.json` returns 1
    - `grep -c '"license": "MIT"' package.json` returns 1
    - `grep -c '"node": ">=20"' package.json` returns 1
    - `grep -c '"files"' package.json` returns 1 and the value contains `"build/"` and `"README.md"`
    - `grep -c '"publishConfig"' package.json` returns 1 and the value contains `"access": "public"`
    - `grep -c '"prefect-mcp": "./build/index.js"' package.json` returns 1
    - `grep -c '"prefect": "./build/cli.js"' package.json` returns 1 (existing entry preserved)
    - `grep -c '"version": "1.0.0"' package.json` returns 1
    - `npm run build` exits 0
    - `npm test` exits 0
    - The phase test gate (D-02) is satisfied - no docs changes have been made yet
  </acceptance_criteria>
  <done>package.json has all publishing fields. Both bin entries present. JSON is valid. Build and tests pass. Wave 1 gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell environment -> MCP server process | User-set env vars (PREFECT_SERVER_PASSWORD, OPENCODE_SERVER_PASSWORD) cross into the process. Existing trust boundary; not altered by rename. |
| MCP server process -> Claude Code stdio (stdout) | JSON-RPC pipe must remain free of stray writes. console.error writes to stderr (safe); console.log would corrupt the protocol. |
| package.json `files` whitelist -> npm registry tarball | Determines which files are published. Misconfiguration could leak source files or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | I (Information disclosure) | package.json `files` field | mitigate | DIST-02 sets `files: ["build/", "README.md"]` whitelist - excludes node_modules/, src/, .planning/, .git/, .mcp.json. Plan 2 verifies via `npm pack --dry-run`. |
| T-09-02 | T (Tampering) | Auth deprecation warning emitting on stdout | mitigate | All deprecation warnings use console.error (Task 2 acceptance includes `grep -c console.log = 0` in modified files). stdout remains pure JSON-RPC. |
| T-09-03 | I (Information disclosure) | Soft-migration fallback reading OPENCODE_SERVER_PASSWORD without warning suppression | accept | Existing user shell configs may have OPENCODE_SERVER_PASSWORD set. Soft migration preserves functionality; one-time warning notifies user to migrate. Token construction (Buffer.from base64) is unchanged - no change in credential handling. Auth credentials still never go to .mcp.json (per existing INFRA-06 README warning, preserved). |
| T-09-04 | E (Elevation of privilege) | Second bin entry `prefect-mcp -> build/index.js` | accept | build/index.js gets +x bit from existing chmod 755 in build script. New bin only exposes existing MCP server functionality at PATH; no new code paths. No privilege boundary change. |
| T-09-05 | D (Denial of service) | Repeated stderr writes from missing warned flag in call-time reads | mitigate | Pattern 2 mandates module-level warned flags for src/auth.ts (warnedPassword, warnedUsername) and src/config.ts (warnedDefaultProject). Without flags, every tool call would print warning - acceptance criteria verify all 3 flags exist. |

Security rationale: This phase is a rename + manifest update. No new attack surfaces are introduced. The dual bin entry is mechanical; the env var rename is transparent. No secrets traverse new paths.
</threat_model>

<verification>
After all tasks complete:

1. `grep -rn "opencode_" src/ --include='*.ts'` exits 1 (zero matches) - DIST-07 satisfied for code
2. `grep -rn "OPENCODE_" src/ --include='*.ts'` matches ONLY the soft-migration fallback IIFEs and deprecation warning strings; no canonical reads or test-file env assignments
3. `npm run build` exits 0
4. `npm test` exits 0 - DIST-08 gate satisfied
5. `node -e "JSON.parse(...)"` on package.json exits 0
6. All publishing fields verifiable in package.json: name=prefect-mcp, description, license, engines, files, publishConfig, dual bin entries
</verification>

<success_criteria>
- All 25 tool name string literals renamed to prefect_* in src/*.ts files
- All 4 OPENCODE_* env var canonical reads converted to PREFECT_* primary + soft-migration fallback
- 3 module-level warned flags exist (warnedPassword, warnedUsername, warnedDefaultProject)
- Test files use canonical new env var names (D-03 satisfied)
- package.json has all publishing fields and dual bin entries
- npm run build exits 0
- npm test exits 0 (D-02 gate satisfied - this is the wave-end test gate)
- No console.log statements added; all deprecation warnings on console.error
- resolveDirectory still returns undefined when no value provided (Phase 5 contract preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/09-npm-distribution/09-01-SUMMARY.md` capturing:
- Files modified (count and names)
- Total opencode_ -> prefect_ replacement count (grep before/after)
- Soft-migration warned flag locations
- Test outcome (npm test output summary)
- Any deviations from the plan (e.g. JSON field ordering, JSDoc text adjustments)
</output>
