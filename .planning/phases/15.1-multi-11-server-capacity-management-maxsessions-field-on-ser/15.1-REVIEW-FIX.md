---
phase: 15.1-multi-11-server-capacity-management-maxsessions-field-on-ser
fixed_at: 2026-05-03T00:00:00Z
review_path: .planning/phases/15.1-multi-11-server-capacity-management-maxsessions-field-on-ser/15.1-REVIEW.md
iteration: 1
fix_scope: warning_info
findings_in_scope: 5
fixed: 2
skipped: 3
status: partial
---

# Phase 15.1: Code Review Fix Report

**Fixed at:** 2026-05-03
**Source review:** .planning/phases/15.1-multi-11-server-capacity-management-maxsessions-field-on-ser/15.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (3 warnings + 2 info)
- Fixed: 2 (WR-02, WR-03)
- Skipped: 3 (WR-01 out of scope — complex; IN-01, IN-02 low priority)

## Fixed Issues

### WR-02: Missing break statements in switch block — fall-through from add-server into remove-server

**Status:** Fixed
**Commit:** 037ec44
**How:** Added `break` after `case 'add-server'`, `case 'remove-server'`, and `case 'list-servers'` in `src/cli.ts`. TypeScript does not warn about this because each handler has return type `never` (calls `process.exit`), but the breaks prevent fall-through if handlers throw before reaching `process.exit`.

### WR-03: parseInt accepts floats for --max-sessions flag

**Status:** Fixed
**Commit:** 037ec44
**How:** Replaced `parseInt`-based validation with a `/^\d+$/` regex check against the raw string, followed by a `< 1` check on the parsed value. This rejects `'1.9'`, `'1.5'`, and other decimal inputs that `parseInt` would silently truncate.

## Skipped Issues

### WR-01: Race condition in capacity enforcement — concurrent session creation overshoots maxSessions

**Status:** Skipped — out of scope
**Reason:** Fixing this correctly requires either atomic read-modify-write for sessions.json (a `reserveSession` function), or a pessimistic advisory lock. Both approaches are a non-trivial refactor affecting `sessions.ts`, `handlers.ts`, and all three capacity-checking call sites in `index.ts`. The race is low-probability in practice (single-process MCP server, sequential tool calls from Claude Code). Deferred to a dedicated locking phase.

### IN-01: removeServer writes to console.error instead of console.log for success confirmation

**Status:** Skipped — low priority
**Reason:** Informational; stdout vs stderr choice is a minor convention issue. Does not affect correctness.

### IN-02: listServers uses console.log while diagnostic output uses console.error

**Status:** Skipped — low priority
**Reason:** listServers intentionally writes to stdout (its output is data, not diagnostics). The inconsistency is real but benign.

---

_Fixed: 2026-05-03_
_Fixer: Claude Code (autonomous fix pass)_
_Iteration: 1_
