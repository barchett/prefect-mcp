/**
 * UAT fixture — committed dummy file for prefect-mcp acceptance tests.
 * Tests use this file to validate symbol indexing, file lookup, content retrieval, and git status.
 * Do not import or use in production code.
 */

export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function farewell(name: string): string {
  return `Goodbye, ${name}!`;
}

export const VERSION = '1.0.0';
